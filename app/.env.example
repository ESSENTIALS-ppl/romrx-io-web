VITE_SUPABASE_URL=https://cqzvqzwwevnflinxgnpp.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# --- Meta Pixel + CAPI (MEASUREMENT OFF until Jim IDs via Grant) ---
# Leave empty = off. Do not invent Pixel IDs or tokens.
# Client Pixel ID (Vite). Empty string keeps fbevents.js from loading.
VITE_META_PIXEL_ID=
# Server-only (Netlify function meta-capi). Never put these in VITE_* / client bundle.
# META_PIXEL_ID=
# META_CAPI_TOKEN=
# Hard flag META_ATTRIBUTION_ENABLED stays false in:
#   app/src/lib/metaAttribution.ts
#   netlify/functions/meta-capi.js
#   assets/meta-attribution.js
# Enable steps: docs/META_ATTRIBUTION_ENABLE.md
